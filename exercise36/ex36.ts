const make_shirt = (size: number | string, message: string) => {
  console.log(
    `The size of t-shirt is ${size} and message written on it is ${message}`
  );
};

make_shirt("XL", "I love doing programming!");
